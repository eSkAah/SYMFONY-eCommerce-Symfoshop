{% extends 'base.html.twig' %}

{% block title %}Modifier mot de passe - SymphoShop{% endblock %}

{% block content %}
    <h1>Modifier mon mot de passe</h1>
    <a href="{{path('account')}}"> Retourner à mon compte</a>
<hr>

{% if notification %}
    <div class="alert alert-warning text-center">{{ notification }}</div>
{% endif %}


{{ form(form) }}

{% endblock %}
